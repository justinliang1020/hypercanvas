// template file for debuggerScript.js
// 1. Run the following command
// `cp debuggerScript.tpl.js debuggerScript.js`
// 2. Modify debuggerScript.js to log whatever you want
/**
 * @param {State} state
 */
export function log(state) {
  console.table(state);
}
